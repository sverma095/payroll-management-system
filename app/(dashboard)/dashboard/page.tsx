export default function DashboardPage() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-semibold text-ink">Dashboard</h1>
      <p className="text-sm text-ink/50 mt-1">
        Overview widgets (headcount, payroll status, pending approvals) land
        here once the Payroll Processing module is built.
      </p>
    </div>
  );
}
