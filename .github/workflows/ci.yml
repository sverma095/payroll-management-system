name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm install
      - run: npx tsc --noEmit
      - run: npx next build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://pmssjdauwuutwuxrhqmz.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: sb_publishable_puVSHuv3RyyYgpaoq7kPWw_M7Pa3Oof
      - run: npx tsx test/formula-engine.test.ts
      - run: npx tsx test/lop-calculator.test.ts
      - run: npx tsx test/tax-and-amortization.test.ts
